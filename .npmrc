# mind-ar pulls in native `canvas` for its offline image compiler only.
# The AR web app uses MindAR's browser bundle at runtime and never needs
# canvas on the server. Skipping install scripts avoids native compile failures
# on Vercel, Windows, and other CI environments.
ignore-scripts=true
